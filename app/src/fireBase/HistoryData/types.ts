export interface IRouteMap {
  series: number;
  idMethod: string;
  isClosed: boolean;
  methodName: string;
  type: string;
  stages: boolean[];
  nameStages: string[];
  temperatures: number[];
  times: number[];
  start: number[];
  shaking: number[];
}
